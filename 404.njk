---
layout: layouts/base-optimized.njk
title: "404 - Page Not Found"
description: "Oops! This page seems to have vanished into the digital void. But don't worry, even the best developers encounter 404s."
eleventyExcludeFromCollections: true
permalink: /404.html
---

<div class="content-section-boxed content-section-top-margin">
    <div class="error-404-container">
        <div class="error-code">
            <h1 class="error-title">
                <span class="error-number">404</span>
                <span class="error-subtitle">Page Not Found</span>
            </h1>
        </div>
        
        <div class="error-message">
            <h2>Houston, we have a problem...</h2>
            <p>The page you're looking for seems to have escaped into the digital void. It's probably off somewhere debugging itself or maybe it joined the millions of other missing semicolons in cyberspace.</p>
        </div>

        <div class="error-humor">
            <div class="code-joke">
                <pre><code><span class="comment">// Last known location of this page:</span>
<span class="keyword">if</span> (pageExists) {
    <span class="string">"You'd be reading it right now"</span>;
} <span class="keyword">else</span> {
    <span class="string">"¯\_(ツ)_/¯"</span>;
}</code></pre>
            </div>
            
            <div class="error-quotes">
                <blockquote>
                    <p>"There are only two hard things in Computer Science: cache invalidation and naming things... and apparently keeping track of web pages."</p>
                    <cite>— Phil Karlton (probably didn't say the last part)</cite>
                </blockquote>
            </div>
        </div>

        <div class="error-suggestions">
            <h3>But hey, while you're here...</h3>
            <div class="suggestion-grid">
                <div class="suggestion-card">
                    <i class="fas fa-home"></i>
                    <h4>Go Home</h4>
                    <p>Return to the safety of the homepage where everything works as expected.</p>
                    <a href="/" class="btn btn-primary">Take Me Home</a>
                </div>
                
                <div class="suggestion-card">
                    <i class="fas fa-blog"></i>
                    <h4>Read the Blog</h4>
                    <p>Check out my adventures in web development, complete with success stories and epic fails.</p>
                    <a href="/blog/" class="btn btn-outline">Browse Posts</a>
                </div>
                
                <div class="suggestion-card">
                    <i class="fas fa-search"></i>
                    <h4>Search Around</h4>
                    <p>Maybe you can find what you're looking for with our super-fast search feature.</p>
                    <a href="/search/" class="btn btn-outline">Start Searching</a>
                </div>
            </div>
        </div>

        <div class="error-footer">
            <div class="error-stats">
                <h4>Fun 404 Facts:</h4>
                <ul>
                    <li>📅 This page has been missing since: <span id="missing-since"></span></li>
                    <li>🔍 Pages searched in vain: <span id="search-count">1,337</span></li>
                    <li>☕ Cups of coffee consumed while debugging: <span id="coffee-count">∞</span></li>
                    <li>🐛 Bugs fixed instead of finding this page: <span id="bugs-fixed">42</span></li>
                </ul>
            </div>
            
            <div class="error-contact">
                <p>If you think this page should exist, <a href="/#contact">let me know</a>! I promise to investigate with the same dedication I bring to hunting down production bugs at 3 AM.</p>
            </div>
        </div>
    </div>
</div>

<style>
.error-404-container {
    text-align: center;
    padding: var(--spacing-2xl) 0;
}

.error-code {
    margin-bottom: var(--spacing-4xl);
}

.error-title {
    margin: 0;
}

.error-number {
    display: block;
    font-size: clamp(6rem, 15vw, 12rem);
    font-weight: var(--font-weight-bold);
    color: var(--color-primary);
    text-shadow: 0 0 20px rgba(25, 212, 254, 0.3);
    line-height: 0.8;
    margin-bottom: var(--spacing-md);
}

.error-subtitle {
    display: block;
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-secondary);
    letter-spacing: var(--letter-spacing-wide);
}

.error-message {
    margin-bottom: var(--spacing-4xl);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
}

.error-message h2 {
    font-size: var(--font-size-xl);
    color: var(--color-primary);
    margin-bottom: var(--spacing-lg);
    font-weight: var(--font-weight-medium);
}

.error-message p {
    font-size: var(--font-size-md);
    line-height: 1.6;
    color: var(--color-text-secondary);
}

.error-humor {
    margin: var(--spacing-4xl) 0;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
}

.code-joke {
    background: var(--color-surface-light);
    border-radius: var(--border-radius-lg);
    padding: var(--spacing-xl);
    margin-bottom: var(--spacing-3xl);
    border: 2px solid var(--color-border);
    text-align: left;
}

.code-joke pre {
    margin: 0;
    font-family: 'Courier New', monospace;
    font-size: var(--font-size-sm);
    line-height: 1.5;
}

.code-joke .comment { color: #6a9955; }
.code-joke .keyword { color: #c586c0; }
.code-joke .string { color: #ce9178; }

.error-quotes blockquote {
    border-left: 4px solid var(--color-primary);
    padding-left: var(--spacing-lg);
    margin: 0;
    font-style: italic;
    font-size: var(--font-size-md);
    color: var(--color-text-secondary);
}

.error-quotes cite {
    display: block;
    margin-top: var(--spacing-sm);
    font-size: var(--font-size-sm);
    color: var(--color-text-tertiary);
    font-style: normal;
}

.error-suggestions {
    margin: var(--spacing-4xl) 0;
}

.error-suggestions h3 {
    font-size: var(--font-size-xl);
    margin-bottom: var(--spacing-3xl);
    color: var(--color-text);
    font-weight: var(--font-weight-medium);
}

.suggestion-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--spacing-xl);
    margin-top: var(--spacing-2xl);
}

.suggestion-card {
    background: var(--color-surface-light);
    border-radius: var(--border-radius-lg);
    padding: var(--spacing-xl);
    border: 2px solid var(--color-border);
    transition: all var(--transition-normal);
}

.suggestion-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-accent-md);
    transform: translateY(-2px);
}

.suggestion-card i {
    font-size: 2.5rem;
    color: var(--color-primary);
    margin-bottom: var(--spacing-lg);
}

.suggestion-card h4 {
    font-size: var(--font-size-lg);
    margin-bottom: var(--spacing-md);
    color: var(--color-text);
    font-weight: var(--font-weight-medium);
}

.suggestion-card p {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-lg);
    line-height: 1.5;
}

.error-footer {
    margin-top: var(--spacing-5xl);
    padding-top: var(--spacing-3xl);
    border-top: 2px solid var(--color-border);
}

.error-stats {
    margin-bottom: var(--spacing-3xl);
}

.error-stats h4 {
    font-size: var(--font-size-lg);
    margin-bottom: var(--spacing-lg);
    color: var(--color-primary);
    font-weight: var(--font-weight-medium);
}

.error-stats ul {
    list-style: none;
    max-width: 500px;
    margin: 0 auto;
}

.error-stats li {
    font-size: var(--font-size-sm);
    margin-bottom: var(--spacing-sm);
    color: var(--color-text-secondary);
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.error-stats span {
    color: var(--color-primary);
    font-weight: var(--font-weight-medium);
    font-family: 'Courier New', monospace;
}

.error-contact {
    max-width: 600px;
    margin: 0 auto;
}

.error-contact p {
    font-size: var(--font-size-sm);
    color: var(--color-text-tertiary);
    line-height: 1.6;
}

.error-contact a {
    color: var(--color-primary);
    text-decoration: none;
    transition: color var(--transition-fast);
}

.error-contact a:hover {
    color: var(--color-primary-dark);
    text-decoration: underline;
}

/* Mobile responsiveness */
@media (max-width: 768px) {
    .error-404-container {
        padding: var(--spacing-xl) 0;
    }
    
    .suggestion-grid {
        grid-template-columns: 1fr;
        gap: var(--spacing-lg);
    }
    
    .error-stats li {
        flex-direction: column;
        align-items: flex-start;
        gap: var(--spacing-xs);
    }
    
    .code-joke {
        padding: var(--spacing-lg);
    }
    
    .code-joke pre {
        font-size: var(--font-size-xs);
    }
}

/* Add some fun animations */
@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
}

.error-number {
    animation: float 3s ease-in-out infinite;
}

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
}

.suggestion-card i {
    animation: pulse 2s ease-in-out infinite;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Set the missing since date to a random past date
    const missingSinceElement = document.getElementById('missing-since');
    if (missingSinceElement) {
        const now = new Date();
        const daysAgo = Math.floor(Math.random() * 30) + 1;
        const missingDate = new Date(now - (daysAgo * 24 * 60 * 60 * 1000));
        missingSinceElement.textContent = missingDate.toLocaleDateString();
    }
    
    // Animate the counters
    function animateCounter(elementId, target, suffix = '') {
        const element = document.getElementById(elementId);
        if (!element || element.textContent === '∞') return;
        
        let current = 0;
        const increment = target / 50;
        const timer = setInterval(() => {
            current += increment;
            if (current >= target) {
                element.textContent = target.toLocaleString() + suffix;
                clearInterval(timer);
            } else {
                element.textContent = Math.floor(current).toLocaleString() + suffix;
            }
        }, 30);
    }
    
    // Animate counters with random values
    setTimeout(() => {
        animateCounter('search-count', Math.floor(Math.random() * 5000) + 1000);
        animateCounter('bugs-fixed', Math.floor(Math.random() * 100) + 20);
    }, 500);
});
</script>

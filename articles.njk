---
layout: layouts/base.njk
title: "Articles"
description: "Technical articles and in-depth content on web development, ColdFusion, and software engineering."
eleventyExcludeFromCollections: true
permalink: /articles/
---

<div class="content-section-boxed content-section-top-margin">
    <div class="page-header">
        <h1 class="section-title">Articles</h1>
        <p class="section-description">In-depth technical articles and explorations into web development, software engineering, and technology trends.</p>
    </div>

    <div class="posts-grid">
        {% set articlesCollection = collections.pages | filterByCategory('articles') %}
        {% if articlesCollection.length > 0 %}
            {% for post in articlesCollection | reverse %}
                <article class="post-card">
                    <div class="post-content">
                        <h2 class="post-title">
                            <a href="{{ post.url }}">{{ post.data.title }}</a>
                        </h2>
                        {% if post.data.description %}
                            <p class="post-excerpt">{{ post.data.description }}</p>
                        {% endif %}
                        {% if post.data.date %}
                            <div class="post-meta">
                                <time datetime="{{ post.data.date | date('iso') }}">
                                    {{ post.data.date | date('MMMM d, yyyy') }}
                                </time>
                            </div>
                        {% endif %}
                    </div>
                </article>
            {% endfor %}
        {% else %}
            <div class="no-posts">
                <p>No articles available yet. Check back soon!</p>
            </div>
        {% endif %}
    </div>
</div>

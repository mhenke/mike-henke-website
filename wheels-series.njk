---
layout: layouts/base.njk
title: "CFWheels Series"
description: "Comprehensive tutorial series on ColdFusion on Wheels framework development."
eleventyExcludeFromCollections: true
permalink: /wheels-series/
---

<div class="content-section-boxed content-section-top-margin">
    <div class="page-header">
        <h1 class="section-title">CFWheels Series</h1>
        <p class="section-description">A comprehensive tutorial series covering ColdFusion on Wheels framework development, from basics to advanced topics.</p>
    </div>

    <div class="posts-grid">
        {% set wheelsCollection = collections.pages | filterByCategory('wheels-series') %}
        {% if wheelsCollection.length > 0 %}
            {% for post in wheelsCollection | reverse %}
                <article class="post-card">
                    <div class="post-content">
                        <h2 class="post-title">
                            <a href="{{ post.url }}">{{ post.data.title }}</a>
                        </h2>
                        {% if post.data.description %}
                            <p class="post-excerpt">{{ post.data.description }}</p>
                        {% endif %}
                        {% if post.data.date %}
                            <div class="post-meta">
                                <time datetime="{{ post.data.date | date('iso') }}">
                                    {{ post.data.date | date('MMMM d, yyyy') }}
                                </time>
                            </div>
                        {% endif %}
                    </div>
                </article>
            {% endfor %}
        {% else %}
            <div class="no-posts">
                <p>No CFWheels series posts available yet. Check back soon!</p>
            </div>
        {% endif %}
    </div>
</div>
